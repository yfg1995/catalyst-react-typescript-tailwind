import { OncologyCro } from "@/components/OncologyCro/OncologyCro";
import { type Metadata } from "next";

export const metadata: Metadata = { title: "Oncology Cro" };

export default function OncologyCroPage() {
  return <OncologyCro />;
}
