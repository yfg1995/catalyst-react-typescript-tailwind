import { CatalystFlex } from "@/components/Flex/CatalystFlex";
import { type Metadata } from "next";

export const metadata: Metadata = { title: "Catalyst Flex" };

export default function CatalystFlexPage() {
  return <CatalystFlex />;
}
