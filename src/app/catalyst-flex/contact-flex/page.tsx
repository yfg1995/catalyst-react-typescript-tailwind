import { ContactFlex } from "@/components/Flex/ContactFlex/ContactFlex";
import { type Metadata } from "next";

export const metadata: Metadata = { title: "Contact Flex" };

export default function ContactFlexPage() {
  return <ContactFlex />;
}
