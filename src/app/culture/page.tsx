import { OurCulture } from "@/components/Home/OurCulture/OurCulture";
import { type Metadata } from "next";

export const metadata: Metadata = { title: "Our Culture" };

export default function OurCulturePage() {
  return <OurCulture />;
}
